spring:
  config:
    activate:
      on-profile: local
external:
  naver:
    url: https://openapi.naver.com
    headers:
      client-id: ${NAVER_API_CLIENT_ID} # 환경변수로 주입을 받아서 실행
      client-secret: ${NAVER_API_CLIENT_SECRET}